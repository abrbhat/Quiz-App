<%= form_for(@quiz_view_log) do |f| %>
  <% if @quiz_view_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quiz_view_log.errors.count, "error") %> prohibited this quiz_view_log from being saved:</h2>

      <ul>
      <% @quiz_view_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quiz_id %><br>
    <%= f.number_field :quiz_id %>
  </div>
  <div class="field">
    <%= f.label :viewer_id %><br>
    <%= f.number_field :viewer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
